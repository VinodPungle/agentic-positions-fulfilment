# docs

- `account-staffing-briefing.html` — the leadership/PM briefing on the agentic fulfilment prototype: what each agent does, the A2A protocol, role-by-role flows, and what's automated vs. manual. Self-contained (fonts and diagrams are inlined, Mermaid renders client-side) — open it directly in any browser, no build step.

To make a manual edit: open the file in any editor and change the relevant `<section>`/`<div class="step">` block directly — the content is plain HTML, organized under clearly commented `<!-- ========================= SECTION ========================= -->` markers. Commit the change like any other file in this repo.
